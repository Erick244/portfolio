import { SkillCardHeader } from "./SkillCardHeader";
import { SkillCardRoot } from "./SkillCardRoot";
import { SkillCardCarousel } from "./carousel";
import { SkillCardContent } from "./content";

export const SkillCard = {
    Root: SkillCardRoot,
    Header: SkillCardHeader,
    Carousel: SkillCardCarousel,
    Content: SkillCardContent,
};
